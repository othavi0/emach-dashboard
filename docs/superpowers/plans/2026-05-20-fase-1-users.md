# Fase 1 — Users Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Elevar a seção `/dashboard/users` ao patamar de `customers`/`orders` usando os primitives da Fase 0. Lista rica com KPIs/pending/activity, detalhe novo com 5 tabs, sheet edit, audit log de ações DO user, e correção de segurança do reset de senha.

**Architecture:**
- Lista refeita usando `EntityKpisRow`, `pending-panel`, `activity-feed`, e `Table` shadcn em vez do `<table>` nativo + cursor-based pagination espelhando `customers`.
- Detalhe novo (`[id]/page.tsx`) com `EntityIdentityHeader` + `EntityTabs` carregando 5 tabs (Perfil, Filiais, Atividade, Sessões, Segurança).
- Edit via `EntityEditSheet` aberto por `?edit=1`.
- Mutations registram em `userActivityLog` via `logUserActivity` (Fase 0).
- `resetUserPassword` (plaintext) é substituído por `triggerPasswordReset` (Better Auth email token).
- Hook do Better Auth atualiza `user.lastLoginAt` em todo sign-in.

**Tech Stack:** Next 16 + React 19 (RSC default), Better Auth dashboard, Drizzle, vitest. Primitives da Fase 0 já em `apps/web/src/components/entity/`.

**Spec:** `docs/superpowers/specs/2026-05-20-users-branches-suppliers-design.md` (Seção "Fase 1 — Users").

---

## File structure

**Novos:**

- `apps/web/src/app/dashboard/users/data.ts` — data fetchers (espelha `customers/data.ts`): `getUserKpis`, `fetchUsersPage`, `getUserDetail`, `getUserSessions`, `getUserActivity`, `fetchPendingUsersPage`, `getRecentUserActivity`.
- `apps/web/src/app/dashboard/users/[id]/page.tsx` — detalhe.
- `apps/web/src/app/dashboard/users/[id]/_components/user-identity.tsx` — wrapper local que monta o EntityIdentityHeader com dados do user (avatar, badges role+status, actions).
- `apps/web/src/app/dashboard/users/[id]/_components/profile-tab.tsx`
- `apps/web/src/app/dashboard/users/[id]/_components/branches-tab.tsx`
- `apps/web/src/app/dashboard/users/[id]/_components/activity-tab.tsx`
- `apps/web/src/app/dashboard/users/[id]/_components/sessions-tab.tsx`
- `apps/web/src/app/dashboard/users/[id]/_components/security-tab.tsx`
- `apps/web/src/app/dashboard/users/_components/users-card-grid.tsx` — usa `EntityCard` + `EntityCardGrid` da Fase 0.
- `apps/web/src/app/dashboard/users/_components/users-filters.tsx` — search + role select + branch select.
- `apps/web/src/app/dashboard/users/_components/users-pending-card.tsx` — usa `PendingPanel` (genérico já existente) com 1 tab "Aprovações".
- `apps/web/src/app/dashboard/users/_components/user-edit-sheet.tsx` — wrap do `EntityEditSheet` com form fields (name/role/branches/status).
- `apps/web/src/app/dashboard/users/_components/role-badge.tsx` — badge com ícone+label por role.
- `apps/web/src/app/dashboard/users/_components/status-badge.tsx` — badge com ícone+label por status (CheckCircle2/Clock/Ban).
- `apps/web/__tests__/users-actions.test.ts` — testes de capability e branch-scoping.

**Modificados:**

- `apps/web/src/app/dashboard/users/page.tsx` — refactor completo (PageHeader + KPIs + pending + activity + filtros + tabs status + grid).
- `apps/web/src/app/dashboard/users/actions.ts` — adicionar `triggerPasswordReset`, `forceLogoutAllSessions`, `revokeUserSession`, `linkUserToBranch`, `unlinkUserFromBranch`. Remover `resetUserPassword` (substituído). Instrumentar todas as mutations com `logUserActivity`.
- `apps/web/src/app/dashboard/users/schema.ts` — adicionar schemas das novas actions.
- `apps/web/src/lib/permissions.ts` — adicionar capability `users.revoke_sessions`. Garantir que `users.manage` cobre list/read.
- `packages/auth/src/dashboard.ts` — hook após sign-in que atualiza `user.lastLoginAt`.

**Renomeados/desativados:**

- `apps/web/src/app/dashboard/users/_components/users-tabs.tsx` — DELETAR após migrar (lista nova usa filtro `?status=...` em vez de Tabs aninhada).
- `apps/web/src/app/dashboard/users/_components/active-table.tsx`, `pending-table.tsx`, `suspended-table.tsx` — DELETAR (substituídos por `users-card-grid.tsx` + `users-pending-card.tsx`).
- `apps/web/src/app/dashboard/users/_components/edit-sheet.tsx` — DELETAR (substituído por `user-edit-sheet.tsx`).
- `apps/web/src/app/dashboard/users/_components/approval-sheet.tsx` — manter (fluxo de aprovação continua próprio).

---

## Polish constraints (Fase 0 carries over)

Reaplicar as restrições da Fase 0 nos novos componentes: surface contrast > borders, lucide-only, status = ícone+label+cor, `font-medium` default, empty states com ícone `size-12 opacity-40`. A skill `impeccable` é invocada na Task 19.

---

### Task 1: Capability `users.revoke_sessions` + tests

**Files:**
- Modify: `apps/web/src/lib/permissions.ts`
- Modify: `apps/web/__tests__/permissions.test.ts`

- [ ] **Step 1: Adicionar capability**

Localizar `Capability` union em `apps/web/src/lib/permissions.ts`. Adicionar `"users.revoke_sessions"` ao union (ordem alfabética junto com os demais `users.*`).

Adicionar ao `ADMIN_CAPS` (ou ao `MANAGER_CAPS` se o spec pediu manager+; o spec diz `users.security → admin+`, então é admin+, NÃO em manager).

Não adicionar em `SUPER_ADMIN_EXCLUSIVE`.

- [ ] **Step 2: Adicionar teste de capability**

Em `apps/web/__tests__/permissions.test.ts`, junto com os testes existentes de `users.*`, adicionar:

```ts
describe("users.revoke_sessions", () => {
	it("admin pode revogar sessões", () => {
		expect(can("admin", "users.revoke_sessions")).toBe(true);
	});
	it("super_admin pode revogar sessões", () => {
		expect(can("super_admin", "users.revoke_sessions")).toBe(true);
	});
	it("manager NÃO pode revogar sessões", () => {
		expect(can("manager", "users.revoke_sessions")).toBe(false);
	});
	it("user NÃO pode revogar sessões", () => {
		expect(can("user", "users.revoke_sessions")).toBe(false);
	});
});
```

- [ ] **Step 3: Rodar testes**

Run: `bun --cwd apps/web test __tests__/permissions.test.ts`. Expected: PASS (existentes + 4 novos).

- [ ] **Step 4: check-types**

Run: `bun --cwd apps/web check-types`. Expected: PASS.

- [ ] **Step 5: Commit**

```bash
git add apps/web/src/lib/permissions.ts apps/web/__tests__/permissions.test.ts
git commit -m "feat(perm): capability users.revoke_sessions"
```

---

### Task 2: Hook Better Auth `lastLoginAt`

**Files:**
- Modify: `packages/auth/src/dashboard.ts`

- [ ] **Step 1: Localizar config do dashboard**

Ler `packages/auth/src/dashboard.ts`. Identificar o `betterAuth({...})` config block.

- [ ] **Step 2: Adicionar hook**

Adicionar/expandir `databaseHooks` (ou `hooks` — checar via `find-docs` Better Auth v1.6 se incerto):

```ts
import { eq } from "drizzle-orm";
import { user as userTable } from "@emach/db/schema/auth";

// dentro do config:
databaseHooks: {
  session: {
    create: {
      after: async (session) => {
        if (session.userId) {
          await db
            .update(userTable)
            .set({ lastLoginAt: new Date() })
            .where(eq(userTable.id, session.userId));
        }
      },
    },
  },
},
```

Se o `databaseHooks.session.create.after` shape não existir nessa versão, usar a alternativa via `hooks.after` matcher (ver docs Better Auth — `find-docs` se necessário).

Se não conseguir confirmar o shape exato, STOP e report NEEDS_CONTEXT.

- [ ] **Step 3: Verificar tipo**

```bash
bun --cwd packages/auth check-types
```

Expected: PASS.

- [ ] **Step 4: Smoke run**

```bash
bun dev:web &
# logar via /login com user existente; depois:
psql $DATABASE_URL -c "SELECT email, last_login_at FROM \"user\" ORDER BY last_login_at DESC NULLS LAST LIMIT 3;"
```

Confirmar que `last_login_at` foi populado para o user que logou. Kill o dev server.

(Se não puder rodar interativamente, pular smoke; o teste de integração é manual após o merge.)

- [ ] **Step 5: Commit**

```bash
git add packages/auth/src/dashboard.ts
git commit -m "feat(auth): atualiza lastLoginAt em sign-in"
```

---

### Task 3: Substituir `resetUserPassword` por `triggerPasswordReset`

**Files:**
- Modify: `apps/web/src/app/dashboard/users/actions.ts`
- Modify: `apps/web/src/app/dashboard/users/schema.ts`

- [ ] **Step 1: Substituir schema**

Em `schema.ts`, REMOVER schema antigo se houver (`resetPasswordSchema`) e adicionar:

```ts
export const triggerPasswordResetSchema = z.object({
	userId: z.string().min(1),
});
export type TriggerPasswordResetInput = z.infer<typeof triggerPasswordResetSchema>;
```

- [ ] **Step 2: Substituir action**

Em `actions.ts`, localizar `resetUserPassword`. Substituir por:

```ts
"use server";

import { auth as authDashboard } from "@emach/auth/dashboard";
import { eq } from "drizzle-orm";
import { db } from "@emach/db";
import { user as userTable } from "@emach/db/schema/auth";

import { logUserActivity } from "@/lib/activity";
import { requireCapability } from "@/lib/permissions";
import { requireCurrentSession } from "@/lib/session";

import { triggerPasswordResetSchema } from "./schema";

export async function triggerPasswordReset(input: unknown): Promise<ActionResult> {
	await requireCapability("users.reset_password");
	const session = await requireCurrentSession();
	const parsed = triggerPasswordResetSchema.safeParse(input);
	if (!parsed.success) {
		return { ok: false, error: "validação" };
	}
	const target = await db.query.user.findFirst({
		where: eq(userTable.id, parsed.data.userId),
	});
	if (!target) {
		return { ok: false, error: "Usuário não encontrado" };
	}
	try {
		await authDashboard.api.requestPasswordReset({
			body: {
				email: target.email,
				redirectTo: `${process.env.BETTER_AUTH_URL}/reset-password`,
			},
		});
		await logUserActivity({
			actorUserId: session.user.id,
			action: "user.password_reset_triggered",
			targetType: "user",
			targetId: parsed.data.userId,
		});
		return { ok: true, data: undefined };
	} catch (err) {
		logger.error({ err, userId: parsed.data.userId }, "triggerPasswordReset");
		return { ok: false, error: "Falha ao enviar e-mail de reset" };
	}
}
```

DELETAR a função `resetUserPassword` antiga inteira (não manter para compatibilidade — nenhum chamador externo deve invocar).

- [ ] **Step 3: Buscar chamadores antigos**

```bash
grep -r "resetUserPassword" apps/web/src
```

Se houver consumidores (`edit-sheet.tsx`, etc), substituir cada chamada por `triggerPasswordReset({userId})`. Não remover botões — só trocar a action; a UX já comunicava "reset password" e o novo fluxo é o mesmo botão emitindo email.

- [ ] **Step 4: check-types**

Run: `bun --cwd apps/web check-types`. Expected: PASS.

- [ ] **Step 5: Commit**

```bash
git add apps/web/src/app/dashboard/users/actions.ts apps/web/src/app/dashboard/users/schema.ts apps/web/src/app/dashboard/users/_components/edit-sheet.tsx
git commit -m "feat(users): triggerPasswordReset via email token"
```

---

### Task 4: Instrumentar mutations existentes com `logUserActivity`

**Files:**
- Modify: `apps/web/src/app/dashboard/users/actions.ts`

- [ ] **Step 1: Localizar cada mutation**

Mutations a instrumentar: `approveUser`, `rejectUser`, `updateUser`, `suspendUser`, `reactivateUser`, `deleteUser`.

Para cada uma, adicionar `await logUserActivity({...})` DEPOIS do commit da transação principal mas ANTES do `revalidatePath`. Action mapeada:

| Mutation | `action` value |
|---|---|
| approveUser | `user.approved` |
| rejectUser | `user.rejected` |
| updateUser | `user.updated` |
| suspendUser | `user.suspended` |
| reactivateUser | `user.reactivated` |
| deleteUser | `user.deleted` |
| (triggerPasswordReset — Task 3) | `user.password_reset_triggered` |

`metadata` carrega:
- approveUser: `{ role, branchIds }`
- rejectUser: `{ reason }`
- updateUser: `{ changes: { ...campos alterados } }` — só os campos não-undefined no input
- suspendUser: `{ reason }`
- reactivateUser: (sem metadata)
- deleteUser: (sem metadata — user está deletado, dados ficam só no log)

Exemplo (approveUser):

```ts
await logUserActivity({
  actorUserId: session.user.id,
  action: "user.approved",
  targetType: "user",
  targetId: parsed.data.userId,
  metadata: { role: parsed.data.role, branchIds: parsed.data.branchIds },
});
```

Garantir import `logUserActivity` no topo do arquivo.

- [ ] **Step 2: check-types**

Run: `bun --cwd apps/web check-types`. PASS.

- [ ] **Step 3: Commit**

```bash
git add apps/web/src/app/dashboard/users/actions.ts
git commit -m "feat(users): instrumenta mutations com logUserActivity"
```

---

### Task 5: Novas actions de sessões

**Files:**
- Modify: `apps/web/src/app/dashboard/users/actions.ts`
- Modify: `apps/web/src/app/dashboard/users/schema.ts`

- [ ] **Step 1: Reaproveitar pattern de `customers`**

Ler `apps/web/src/app/dashboard/customers/actions.ts` para encontrar como funcionam `revokeClientSession` e `revokeAllClientSessions` (qualquer nome que use). Copiar o pattern adaptando para `session` table do Better Auth dashboard (em vez de `clientSession`).

Better Auth dashboard sessions estão em `packages/db/src/schema/auth.ts` → `session` table. Filtros: `session.userId`, `session.expiresAt > now()`.

- [ ] **Step 2: Schema**

Em `schema.ts`:

```ts
export const revokeSessionSchema = z.object({
	sessionId: z.string().min(1),
});
export type RevokeSessionInput = z.infer<typeof revokeSessionSchema>;
```

- [ ] **Step 3: Actions**

Em `actions.ts`:

```ts
import { session as sessionTable } from "@emach/db/schema/auth";
import { and, eq, gt } from "drizzle-orm";

export async function revokeUserSession(input: unknown): Promise<ActionResult> {
	await requireCapability("users.revoke_sessions");
	const actor = await requireCurrentSession();
	const parsed = revokeSessionSchema.safeParse(input);
	if (!parsed.success) return { ok: false, error: "validação" };

	const target = await db.query.session.findFirst({
		where: eq(sessionTable.id, parsed.data.sessionId),
	});
	if (!target) return { ok: false, error: "Sessão não encontrada" };

	await db.delete(sessionTable).where(eq(sessionTable.id, parsed.data.sessionId));
	await logUserActivity({
		actorUserId: actor.user.id,
		action: "user.session_revoked",
		targetType: "user",
		targetId: target.userId,
		metadata: { sessionId: target.id },
	});
	revalidatePath(`/dashboard/users/${target.userId}`);
	return { ok: true, data: undefined };
}

export async function forceLogoutAllSessions(input: unknown): Promise<ActionResult> {
	await requireCapability("users.revoke_sessions");
	const actor = await requireCurrentSession();
	const parsed = userIdSchema.safeParse(input);
	if (!parsed.success) return { ok: false, error: "validação" };

	const deleted = await db
		.delete(sessionTable)
		.where(eq(sessionTable.userId, parsed.data.userId))
		.returning({ id: sessionTable.id });

	await logUserActivity({
		actorUserId: actor.user.id,
		action: "user.all_sessions_revoked",
		targetType: "user",
		targetId: parsed.data.userId,
		metadata: { count: deleted.length },
	});
	revalidatePath(`/dashboard/users/${parsed.data.userId}`);
	return { ok: true, data: { count: deleted.length } };
}
```

- [ ] **Step 4: check-types**

PASS.

- [ ] **Step 5: Commit**

```bash
git add apps/web/src/app/dashboard/users/actions.ts apps/web/src/app/dashboard/users/schema.ts
git commit -m "feat(users): revokeUserSession + forceLogoutAllSessions"
```

---

### Task 6: Actions granulares de vínculo de filial

**Files:**
- Modify: `apps/web/src/app/dashboard/users/actions.ts`
- Modify: `apps/web/src/app/dashboard/users/schema.ts`

- [ ] **Step 1: Schema**

```ts
export const branchLinkSchema = z.object({
	userId: z.string().min(1),
	branchId: z.string().min(1),
});
export type BranchLinkInput = z.infer<typeof branchLinkSchema>;
```

- [ ] **Step 2: Actions**

```ts
import { userBranch } from "@emach/db/schema/inventory";

export async function linkUserToBranch(input: unknown): Promise<ActionResult> {
	await requireCapability("users.update_branches");
	const actor = await requireCurrentSession();
	const parsed = branchLinkSchema.safeParse(input);
	if (!parsed.success) return { ok: false, error: "validação" };

	await db.insert(userBranch).values({
		userId: parsed.data.userId,
		branchId: parsed.data.branchId,
	}).onConflictDoNothing();

	await logUserActivity({
		actorUserId: actor.user.id,
		action: "user.branch_linked",
		targetType: "user",
		targetId: parsed.data.userId,
		metadata: { branchId: parsed.data.branchId },
	});
	revalidatePath(`/dashboard/users/${parsed.data.userId}`);
	return { ok: true, data: undefined };
}

export async function unlinkUserFromBranch(input: unknown): Promise<ActionResult> {
	await requireCapability("users.update_branches");
	const actor = await requireCurrentSession();
	const parsed = branchLinkSchema.safeParse(input);
	if (!parsed.success) return { ok: false, error: "validação" };

	// Guard: don't unlink if it would leave an admin with zero branches managing a branch where they were the last admin — defer this check; for now allow any unlink (TODO note).
	await db.delete(userBranch).where(
		and(
			eq(userBranch.userId, parsed.data.userId),
			eq(userBranch.branchId, parsed.data.branchId),
		),
	);

	await logUserActivity({
		actorUserId: actor.user.id,
		action: "user.branch_unlinked",
		targetType: "user",
		targetId: parsed.data.userId,
		metadata: { branchId: parsed.data.branchId },
	});
	revalidatePath(`/dashboard/users/${parsed.data.userId}`);
	return { ok: true, data: undefined };
}
```

Não adicionar o guard de "último admin" agora — registrar como TODO no spec do PR. Trade-off: a UI já permite remover branches via `updateUser`, então o gap é simétrico.

- [ ] **Step 3: check-types**

PASS.

- [ ] **Step 4: Commit**

```bash
git add apps/web/src/app/dashboard/users/actions.ts apps/web/src/app/dashboard/users/schema.ts
git commit -m "feat(users): link/unlinkUserToBranch granulares"
```

---

### Task 7: `data.ts` — fetchers

**Files:**
- Create: `apps/web/src/app/dashboard/users/data.ts`

- [ ] **Step 1: Estrutura inicial**

Espelhar `apps/web/src/app/dashboard/customers/data.ts` (cursor-based) e adaptar para users.

Fetchers:

```ts
import "server-only";

import { auth as authDashboard } from "@emach/auth/dashboard";
import { db } from "@emach/db";
import { user as userTable, session as sessionTable } from "@emach/db/schema/auth";
import { branch, userBranch } from "@emach/db/schema/inventory";
import { userActivityLog } from "@emach/db/schema/user-activity";
import {
	and,
	asc,
	desc,
	eq,
	gt,
	ilike,
	inArray,
	isNotNull,
	lte,
	or,
	sql,
} from "drizzle-orm";

import type { InfiniteResult } from "@/lib/infinite";

export interface UserKpis {
	active: number;
	pending: number;
	suspended: number;
	branchesCovered: number;
}

export async function getUserKpis(): Promise<UserKpis> {
	const [counts] = await db
		.select({
			active: sql<number>`count(*) filter (where ${userTable.status} = 'active')::int`,
			pending: sql<number>`count(*) filter (where ${userTable.status} = 'pending')::int`,
			suspended: sql<number>`count(*) filter (where ${userTable.status} = 'suspended')::int`,
		})
		.from(userTable);
	const [branchesCovered] = await db
		.select({ n: sql<number>`count(distinct ${userBranch.branchId})::int` })
		.from(userBranch);
	return {
		active: counts?.active ?? 0,
		pending: counts?.pending ?? 0,
		suspended: counts?.suspended ?? 0,
		branchesCovered: branchesCovered?.n ?? 0,
	};
}

export interface UserListRow {
	id: string;
	name: string;
	email: string;
	role: "super_admin" | "admin" | "manager" | "user";
	status: "pending" | "active" | "suspended";
	image: string | null;
	branchIds: string[];
	branchNames: string[];
	createdAt: Date;
	lastLoginAt: Date | null;
}

export interface UserListFilters {
	status?: "active" | "pending" | "suspended";
	role?: "super_admin" | "admin" | "manager" | "user";
	branchId?: string;
	search?: string;
	cursor?: string | null;
	limit?: number;
}

export async function fetchUsersPage(
	filters: UserListFilters
): Promise<InfiniteResult<UserListRow>> {
	const limit = filters.limit ?? 24;
	const cursorDate = filters.cursor ? new Date(filters.cursor) : null;

	const whereParts = [];
	if (filters.status) whereParts.push(eq(userTable.status, filters.status));
	if (filters.role) whereParts.push(eq(userTable.role, filters.role));
	if (filters.search) {
		whereParts.push(
			or(
				ilike(userTable.name, `%${filters.search}%`),
				ilike(userTable.email, `%${filters.search}%`)
			)
		);
	}
	if (filters.branchId) {
		whereParts.push(
			sql`${userTable.id} IN (SELECT ${userBranch.userId} FROM ${userBranch} WHERE ${userBranch.branchId} = ${filters.branchId})`
		);
	}
	if (cursorDate) whereParts.push(lte(userTable.createdAt, cursorDate));

	const rows = await db
		.select({
			id: userTable.id,
			name: userTable.name,
			email: userTable.email,
			role: userTable.role,
			status: userTable.status,
			image: userTable.image,
			createdAt: userTable.createdAt,
			lastLoginAt: userTable.lastLoginAt,
			branchIds: sql<string[]>`coalesce(array_agg(${userBranch.branchId}) filter (where ${userBranch.branchId} is not null), '{}')`,
			branchNames: sql<string[]>`coalesce(array_agg(${branch.name}) filter (where ${branch.id} is not null), '{}')`,
		})
		.from(userTable)
		.leftJoin(userBranch, eq(userBranch.userId, userTable.id))
		.leftJoin(branch, eq(branch.id, userBranch.branchId))
		.where(whereParts.length ? and(...whereParts) : undefined)
		.groupBy(userTable.id)
		.orderBy(desc(userTable.createdAt))
		.limit(limit + 1);

	const hasMore = rows.length > limit;
	const items = (hasMore ? rows.slice(0, limit) : rows) as UserListRow[];
	const nextCursor = hasMore
		? items.at(-1)?.createdAt.toISOString() ?? null
		: null;

	return { items, hasMore, cursor: nextCursor };
}

export async function fetchPendingUsersPage(
	cursor: string | null
): Promise<InfiniteResult<{
	id: string;
	primary: string;
	secondary: string;
	href: string;
}>> {
	const cursorDate = cursor ? new Date(cursor) : null;
	const rows = await db
		.select({
			id: userTable.id,
			name: userTable.name,
			email: userTable.email,
			createdAt: userTable.createdAt,
		})
		.from(userTable)
		.where(
			and(
				eq(userTable.status, "pending"),
				cursorDate ? lte(userTable.createdAt, cursorDate) : undefined
			)
		)
		.orderBy(desc(userTable.createdAt))
		.limit(20);

	return {
		items: rows.map((r) => ({
			id: r.id,
			primary: r.name,
			secondary: r.email,
			href: `/dashboard/users/${r.id}`,
		})),
		hasMore: false,
		cursor: null,
	};
}

export interface UserDetail extends UserListRow {
	emailVerified: boolean | null;
}

export async function getUserDetail(id: string): Promise<UserDetail | null> {
	const [row] = await db
		.select({
			id: userTable.id,
			name: userTable.name,
			email: userTable.email,
			emailVerified: userTable.emailVerified,
			role: userTable.role,
			status: userTable.status,
			image: userTable.image,
			createdAt: userTable.createdAt,
			lastLoginAt: userTable.lastLoginAt,
			branchIds: sql<string[]>`coalesce(array_agg(${userBranch.branchId}) filter (where ${userBranch.branchId} is not null), '{}')`,
			branchNames: sql<string[]>`coalesce(array_agg(${branch.name}) filter (where ${branch.id} is not null), '{}')`,
		})
		.from(userTable)
		.leftJoin(userBranch, eq(userBranch.userId, userTable.id))
		.leftJoin(branch, eq(branch.id, userBranch.branchId))
		.where(eq(userTable.id, id))
		.groupBy(userTable.id);
	return (row as UserDetail) ?? null;
}

export async function getUserSessions(userId: string) {
	return await db
		.select({
			id: sessionTable.id,
			ipAddress: sessionTable.ipAddress,
			userAgent: sessionTable.userAgent,
			createdAt: sessionTable.createdAt,
			expiresAt: sessionTable.expiresAt,
		})
		.from(sessionTable)
		.where(
			and(
				eq(sessionTable.userId, userId),
				gt(sessionTable.expiresAt, new Date())
			)
		)
		.orderBy(desc(sessionTable.createdAt));
}

export async function getUserActivity(
	userId: string,
	cursor: string | null,
	limit = 25
): Promise<InfiniteResult<{
	id: string;
	action: string;
	targetType: string | null;
	targetId: string | null;
	metadata: Record<string, unknown> | null;
	createdAt: Date;
}>> {
	const cursorDate = cursor ? new Date(cursor) : null;
	const rows = await db
		.select()
		.from(userActivityLog)
		.where(
			and(
				eq(userActivityLog.actorUserId, userId),
				cursorDate ? lte(userActivityLog.createdAt, cursorDate) : undefined
			)
		)
		.orderBy(desc(userActivityLog.createdAt))
		.limit(limit + 1);

	const hasMore = rows.length > limit;
	const items = hasMore ? rows.slice(0, limit) : rows;
	return {
		items: items.map((r) => ({
			id: r.id,
			action: r.action,
			targetType: r.targetType,
			targetId: r.targetId,
			metadata: r.metadata as Record<string, unknown> | null,
			createdAt: r.createdAt,
		})),
		hasMore,
		cursor: hasMore ? items.at(-1)?.createdAt.toISOString() ?? null : null,
	};
}

export async function getRecentUserActivity(limit = 8) {
	return await db
		.select({
			id: userActivityLog.id,
			action: userActivityLog.action,
			actorName: userTable.name,
			targetId: userActivityLog.targetId,
			createdAt: userActivityLog.createdAt,
		})
		.from(userActivityLog)
		.leftJoin(userTable, eq(userTable.id, userActivityLog.actorUserId))
		.where(ilike(userActivityLog.action, "user.%"))
		.orderBy(desc(userActivityLog.createdAt))
		.limit(limit);
}
```

- [ ] **Step 2: check-types**

PASS.

- [ ] **Step 3: Commit**

```bash
git add apps/web/src/app/dashboard/users/data.ts
git commit -m "feat(users): data fetchers (kpis, list, detail, activity)"
```

---

### Task 8: Lista page — refactor inicial (KPIs + filters + grid)

**Files:**
- Modify: `apps/web/src/app/dashboard/users/page.tsx`
- Create: `apps/web/src/app/dashboard/users/_components/users-filters.tsx`
- Create: `apps/web/src/app/dashboard/users/_components/users-card-grid.tsx`
- Create: `apps/web/src/app/dashboard/users/_components/users-pending-card.tsx`
- Create: `apps/web/src/app/dashboard/users/_components/role-badge.tsx`
- Create: `apps/web/src/app/dashboard/users/_components/status-badge.tsx`

- [ ] **Step 1: Badges**

`role-badge.tsx` (server component):

```tsx
import { Badge } from "@emach/ui/components/badge";
import { Crown, Shield, ShieldCheck, UserRound } from "lucide-react";

const META: Record<string, { label: string; icon: typeof Crown; variant?: "default" | "secondary" | "outline" }> = {
	super_admin: { label: "Super Admin", icon: Crown },
	admin: { label: "Admin", icon: ShieldCheck },
	manager: { label: "Manager", icon: Shield, variant: "secondary" },
	user: { label: "Estoquista", icon: UserRound, variant: "outline" },
};

export function RoleBadge({ role }: { role: string }) {
	const m = META[role] ?? META.user;
	const Icon = m.icon;
	return (
		<Badge variant={m.variant}>
			<Icon aria-hidden className="mr-1 size-3" />
			{m.label}
		</Badge>
	);
}
```

`status-badge.tsx`:

```tsx
import { Badge } from "@emach/ui/components/badge";
import { Ban, CheckCircle2, Clock } from "lucide-react";

const META = {
	active: { label: "Ativo", icon: CheckCircle2, variant: "default" as const },
	pending: { label: "Pendente", icon: Clock, variant: "warning" as const },
	suspended: { label: "Suspenso", icon: Ban, variant: "destructive" as const },
};

export function StatusBadge({ status }: { status: keyof typeof META }) {
	const m = META[status];
	const Icon = m.icon;
	return (
		<Badge variant={m.variant}>
			<Icon aria-hidden className="mr-1 size-3" />
			{m.label}
		</Badge>
	);
}
```

- [ ] **Step 2: `users-filters.tsx` (Client Component)**

Espelhar `customer-filters.tsx`. Inputs: search (debounced), role select, branch select. Persiste via `?search=&role=&branchId=&status=` no URL via `router.replace`.

(Code shape detalhado: ler `customer-filters.tsx` e adaptar — encurtando aqui para manter o plano gerenciável.)

- [ ] **Step 3: `users-card-grid.tsx` (Client Component)**

Usa `EntityCardGrid` + `EntityCard` da Fase 0. Recebe initial items + `fetchPage` para infinite. Cada card:

```tsx
<EntityCard
  avatarUrl={user.image}
  avatarFallback={initials(user.name)}
  title={user.name}
  subtitle={user.email}
  badges={<><RoleBadge role={user.role} /><StatusBadge status={user.status} /></>}
  meta={user.branchNames.length ? user.branchNames.slice(0, 2).join(" · ") + (user.branchNames.length > 2 ? ` +${user.branchNames.length - 2}` : "") : "Sem filial"}
  footer={<>
    <span className="text-muted-foreground text-xs">
      {user.lastLoginAt ? `Login ${formatRelative(user.lastLoginAt)}` : "Nunca logou"}
    </span>
    <Button asChild size="sm" variant="outline">
      <Link href={`/dashboard/users/${user.id}`}><Eye className="size-3.5" /> Ver</Link>
    </Button>
  </>}
  href={`/dashboard/users/${user.id}`}
/>
```

Helper `initials(name)`: pegar 2 primeiras letras das primeira e última palavra do nome.

`formatRelative(date)`: usar `Intl.RelativeTimeFormat` ou helper já existente em `apps/web/src/lib/` (procurar antes de criar).

- [ ] **Step 4: `users-pending-card.tsx`**

Usa `PendingPanel` (já genérico em `apps/web/src/components/pending-panel.tsx`) com 1 tab "Aprovações". Passa `fetchPendingUsersPage` como `fetchPage`.

- [ ] **Step 5: Refactor `page.tsx`**

```tsx
import { Suspense } from "react";

import { ActivityFeed } from "@/components/activity-feed";
import { EntityKpisRow } from "@/components/entity/entity-kpis-row";
import { PageHeader } from "@/components/page-header";
import { requireCapabilityOrRedirect } from "@/lib/permissions";

import { fetchPendingUsersPage, fetchUsersPage, getRecentUserActivity, getUserKpis } from "./data";
import { UsersCardGrid } from "./_components/users-card-grid";
import { UsersFilters } from "./_components/users-filters";
import { UsersPendingCard } from "./_components/users-pending-card";

export const dynamic = "force-dynamic";

interface PageProps {
	searchParams: Promise<{
		status?: string;
		role?: string;
		branchId?: string;
		search?: string;
	}>;
}

export default async function UsersPage({ searchParams }: PageProps) {
	await requireCapabilityOrRedirect("users.manage");
	const sp = await searchParams;

	const filters = {
		status: (sp.status as "active" | "pending" | "suspended" | undefined) ?? "active",
		role: sp.role as never,
		branchId: sp.branchId,
		search: sp.search,
	};

	const [kpis, page, pending, activity, branches] = await Promise.all([
		getUserKpis(),
		fetchUsersPage(filters),
		fetchPendingUsersPage(null),
		getRecentUserActivity(),
		// TODO: branches list — for filter select
		Promise.resolve([]),
	]);

	return (
		<div className="flex flex-col gap-6">
			<PageHeader title="Usuários" description="Equipe interna do Emach" />
			<EntityKpisRow
				items={[
					{ label: "Ativos", value: kpis.active, icon: CheckCircle2 },
					{
						label: "Pendentes",
						value: kpis.pending,
						tone: kpis.pending > 0 ? "warning" : "default",
						icon: Clock,
						href: "?status=pending",
					},
					{ label: "Suspensos", value: kpis.suspended, icon: Ban },
					{ label: "Filiais cobertas", value: kpis.branchesCovered, icon: Building2 },
				]}
			/>
			<div className="grid grid-cols-1 gap-3 lg:grid-cols-2">
				<UsersPendingCard initial={pending.items} initialCursor={pending.cursor} count={kpis.pending} />
				<ActivityFeed
					initialEvents={activity.map((a) => ({
						id: a.id,
						kind: "user",
						primary: humanizeAction(a.action, a.actorName ?? "—"),
						at: a.createdAt,
						href: a.targetId ? `/dashboard/users/${a.targetId}` : undefined,
					}))}
					initialCursor={null}
					fetchPage={async () => ({ items: [], hasMore: false, cursor: null })}
				/>
			</div>
			<UsersFilters branches={branches} />
			<UsersCardGrid
				key={JSON.stringify(filters)}
				initialItems={page.items}
				initialCursor={page.cursor}
				filters={filters}
			/>
		</div>
	);
}
```

Helper `humanizeAction(action, actorName)`: switch case sobre `user.approved | user.suspended | user.password_reset_triggered | ...` retornando string em PT.

- [ ] **Step 6: check-types + smoke**

```bash
bun --cwd apps/web check-types
bun dev:web &
# visitar /dashboard/users
```

- [ ] **Step 7: Commit**

```bash
git add apps/web/src/app/dashboard/users/
git commit -m "feat(users): refactor lista (KPIs + pending + grid)"
```

---

### Task 9: Status tabs + cursor pagination na lista

**Files:**
- Modify: `apps/web/src/app/dashboard/users/page.tsx`
- Modify: `apps/web/src/app/dashboard/users/_components/users-card-grid.tsx`

- [ ] **Step 1: Adicionar status tabs**

Em `page.tsx`, entre `UsersFilters` e `UsersCardGrid`, adicionar:

```tsx
<Tabs value={filters.status ?? "active"}>
  <TabsList>
    <TabsTrigger asChild value="active"><Link href={updateSearch(sp, { status: "active" })}>Ativos ({kpis.active})</Link></TabsTrigger>
    <TabsTrigger asChild value="pending"><Link href={updateSearch(sp, { status: "pending" })}>Pendentes <Badge>{kpis.pending}</Badge></Link></TabsTrigger>
    <TabsTrigger asChild value="suspended"><Link href={updateSearch(sp, { status: "suspended" })}>Suspensos ({kpis.suspended})</Link></TabsTrigger>
  </TabsList>
</Tabs>
```

`updateSearch(sp, patch)` — helper que cria nova querystring. Pode reusar de `customers` se já houver, senão criar inline.

- [ ] **Step 2: Cursor pagination no `users-card-grid.tsx`**

Usar `useInfiniteList` (já existente em `@/lib/use-infinite-list`) + `InfiniteSentinel`. `fetchPage` chama uma server action `fetchMoreUsers(filters, cursor)` que é exposta de `actions.ts` (precisa criar):

```ts
"use server";
import { fetchUsersPage, type UserListFilters } from "./data";

export async function fetchMoreUsersAction(
	filters: UserListFilters,
	cursor: string | null
) {
	await requireCapability("users.manage");
	return fetchUsersPage({ ...filters, cursor });
}
```

- [ ] **Step 3: check-types + smoke**

PASS.

- [ ] **Step 4: Commit**

```bash
git add apps/web/src/app/dashboard/users/
git commit -m "feat(users): status tabs + cursor pagination na lista"
```

---

### Task 10: Detalhe `[id]/page.tsx` — esqueleto

**Files:**
- Create: `apps/web/src/app/dashboard/users/[id]/page.tsx`
- Create: `apps/web/src/app/dashboard/users/[id]/_components/user-identity.tsx`

- [ ] **Step 1: `user-identity.tsx` (Client Component — tem botões)**

```tsx
"use client";

import { Button } from "@emach/ui/components/button";
import { useRouter, useSearchParams } from "next/navigation";

import { EntityIdentityHeader } from "@/components/entity/entity-identity-header";
import type { UserDetail } from "../../data";

import { RoleBadge } from "../../_components/role-badge";
import { StatusBadge } from "../../_components/status-badge";

interface Props {
	user: UserDetail;
}

export function UserIdentity({ user }: Props) {
	const router = useRouter();
	const params = useSearchParams();
	const openEdit = () => {
		const sp = new URLSearchParams(params);
		sp.set("edit", "1");
		router.replace(`?${sp.toString()}`, { scroll: false });
	};
	const initials = user.name
		.split(" ")
		.filter(Boolean)
		.slice(0, 2)
		.map((p) => p[0]?.toUpperCase() ?? "")
		.join("");
	return (
		<EntityIdentityHeader
			avatarUrl={user.image}
			avatarFallback={initials || "?"}
			title={user.name}
			subtitle={user.email}
			badges={
				<>
					<RoleBadge role={user.role} />
					<StatusBadge status={user.status} />
				</>
			}
			actions={
				<>
					<Button onClick={openEdit}>Editar</Button>
				</>
			}
		/>
	);
}
```

(Mais botões — Reset senha, Suspender etc — virão em tasks específicas das tabs Segurança/Perfil. Manter mínimo aqui.)

- [ ] **Step 2: `[id]/page.tsx`**

```tsx
import { notFound } from "next/navigation";

import { EntityTabs } from "@/components/entity/entity-tabs";
import { requireCapabilityOrRedirect } from "@/lib/permissions";

import { getUserDetail } from "../data";
import { ActivityTab } from "./_components/activity-tab";
import { BranchesTab } from "./_components/branches-tab";
import { ProfileTab } from "./_components/profile-tab";
import { SecurityTab } from "./_components/security-tab";
import { SessionsTab } from "./_components/sessions-tab";
import { UserIdentity } from "./_components/user-identity";

interface PageProps {
	params: Promise<{ id: string }>;
	searchParams: Promise<{ tab?: string; edit?: string }>;
}

export default async function UserDetailPage({ params, searchParams }: PageProps) {
	await requireCapabilityOrRedirect("users.manage");
	const { id } = await params;
	const sp = await searchParams;

	const user = await getUserDetail(id);
	if (!user) {
		notFound();
	}

	return (
		<div className="flex flex-col gap-6">
			<UserIdentity user={user} />
			<EntityTabs
				defaultValue="profile"
				tabs={[
					{ value: "profile", label: "Perfil", content: <ProfileTab user={user} /> },
					{ value: "branches", label: "Filiais", content: <BranchesTab user={user} /> },
					{ value: "activity", label: "Atividade", content: <ActivityTab userId={user.id} /> },
					{ value: "sessions", label: "Sessões", content: <SessionsTab userId={user.id} /> },
					{ value: "security", label: "Segurança", content: <SecurityTab user={user} /> },
				]}
			/>
			{/* Sheet edit aparece via ?edit=1 — implementado em task posterior */}
		</div>
	);
}
```

⚠ Nota: como `EntityTabs` renderiza TODOS os contents (toggling visibility no client), cada tab que faz fetch precisa lidar com isso. Decisão pragmática: server-render tudo aqui (cada Tab é Server Component) — load inicial puxa todos os dados. Para Atividade/Sessões que podem ser pesados, usar `<Suspense>` com fallback skeleton em cada Tab que faz fetch async.

- [ ] **Step 3: Criar stubs para as 5 tabs**

Cada um (`profile-tab.tsx`, `branches-tab.tsx`, etc): por enquanto, um componente que retorna `<p>TODO</p>` para destravar a compilação. As implementações reais vêm em Tasks 11–15.

```tsx
// profile-tab.tsx
import type { UserDetail } from "../../data";
export function ProfileTab({ user }: { user: UserDetail }) {
	return <p className="text-muted-foreground text-sm">TODO ProfileTab — {user.email}</p>;
}
```

Repetir para `branches-tab.tsx`, `activity-tab.tsx`, `sessions-tab.tsx`, `security-tab.tsx` com signatures correspondentes.

- [ ] **Step 4: check-types + smoke**

`bun --cwd apps/web check-types` PASS. Smoke: visitar `/dashboard/users/<id>` (use um id real do seed). Tab switching atualiza `?tab=...` na URL.

- [ ] **Step 5: Commit**

```bash
git add apps/web/src/app/dashboard/users/[id]/
git commit -m "feat(users): detalhe page + 5 tabs stub"
```

---

### Task 11: ProfileTab

**Files:**
- Modify: `apps/web/src/app/dashboard/users/[id]/_components/profile-tab.tsx`

- [ ] **Step 1: Implementar**

```tsx
import { Card, CardContent, CardHeader, CardTitle } from "@emach/ui/components/card";

import type { UserDetail } from "../../data";

const DATE = new Intl.DateTimeFormat("pt-BR", {
	dateStyle: "long",
	timeStyle: "short",
});

function Field({ label, value }: { label: string; value: React.ReactNode }) {
	return (
		<div>
			<dt className="text-muted-foreground text-xs uppercase tracking-wide">
				{label}
			</dt>
			<dd className="font-medium text-sm">{value}</dd>
		</div>
	);
}

export function ProfileTab({ user }: { user: UserDetail }) {
	return (
		<Card>
			<CardHeader>
				<CardTitle className="text-base">Perfil</CardTitle>
			</CardHeader>
			<CardContent>
				<dl className="grid grid-cols-1 gap-4 sm:grid-cols-2">
					<Field label="Nome" value={user.name} />
					<Field label="Email" value={user.email} />
					<Field label="Email verificado" value={user.emailVerified ? "Sim" : "Não"} />
					<Field label="Cadastrado em" value={DATE.format(user.createdAt)} />
					<Field
						label="Último login"
						value={user.lastLoginAt ? DATE.format(user.lastLoginAt) : "Nunca"}
					/>
					<Field
						label="Filiais"
						value={
							user.branchNames.length > 0
								? user.branchNames.join(" · ")
								: "Sem filial vinculada"
						}
					/>
				</dl>
			</CardContent>
		</Card>
	);
}
```

- [ ] **Step 2: check-types + smoke** PASS.

- [ ] **Step 3: Commit**

```bash
git add apps/web/src/app/dashboard/users/[id]/_components/profile-tab.tsx
git commit -m "feat(users): ProfileTab"
```

---

### Task 12: BranchesTab

**Files:**
- Modify: `apps/web/src/app/dashboard/users/[id]/_components/branches-tab.tsx`

- [ ] **Step 1: Estrutura**

Server Component que carrega branches disponíveis. Renderiza:
- Lista de branches vinculadas (cards leves) + botão "Desvincular" em cada.
- Combobox no topo para vincular nova (chama `linkUserToBranch`).
- Empty state se 0 filiais.

```tsx
"use client";

import { Button } from "@emach/ui/components/button";
import { Card, CardContent, CardHeader, CardTitle } from "@emach/ui/components/card";
import { Building2, Plus, X } from "lucide-react";
import { useTransition } from "react";
import { toast } from "sonner";

import {
	linkUserToBranch,
	unlinkUserFromBranch,
} from "../../actions";

interface Branch { id: string; name: string }
interface Props {
	user: { id: string; branchIds: string[]; branchNames: string[] };
	availableBranches: Branch[];
}

export function BranchesTab({ user, availableBranches }: Props) {
	const [pending, startTransition] = useTransition();
	const linked = user.branchIds.map((id, i) => ({
		id,
		name: user.branchNames[i] ?? id,
	}));
	const unlinkedOptions = availableBranches.filter(
		(b) => !user.branchIds.includes(b.id)
	);

	const link = (branchId: string) =>
		startTransition(async () => {
			const res = await linkUserToBranch({ userId: user.id, branchId });
			if (!res.ok) toast.error(res.error);
		});
	const unlink = (branchId: string) =>
		startTransition(async () => {
			const res = await unlinkUserFromBranch({ userId: user.id, branchId });
			if (!res.ok) toast.error(res.error);
		});

	return (
		<Card>
			<CardHeader>
				<CardTitle className="text-base">Filiais vinculadas</CardTitle>
			</CardHeader>
			<CardContent className="flex flex-col gap-3">
				{linked.length === 0 ? (
					<div className="flex flex-col items-center gap-2 py-8 text-center">
						<Building2 className="size-12 text-muted-foreground opacity-40" />
						<p className="font-medium text-sm">Sem filiais vinculadas</p>
					</div>
				) : (
					<ul className="flex flex-col gap-2">
						{linked.map((b) => (
							<li
								key={b.id}
								className="flex items-center justify-between rounded-md bg-muted/40 px-3 py-2"
							>
								<span className="text-sm">{b.name}</span>
								<Button
									disabled={pending}
									onClick={() => unlink(b.id)}
									size="sm"
									variant="ghost"
								>
									<X className="size-3.5" />
									Desvincular
								</Button>
							</li>
						))}
					</ul>
				)}
				{unlinkedOptions.length > 0 ? (
					<div className="flex flex-wrap gap-2 border-border border-t pt-3">
						{unlinkedOptions.map((b) => (
							<Button
								disabled={pending}
								key={b.id}
								onClick={() => link(b.id)}
								size="sm"
								variant="outline"
							>
								<Plus className="size-3.5" />
								{b.name}
							</Button>
						))}
					</div>
				) : null}
			</CardContent>
		</Card>
	);
}
```

Para passar `availableBranches`, ajustar `[id]/page.tsx` para buscar `db.select... from branch` e passar como prop.

- [ ] **Step 2: check-types + smoke**

PASS.

- [ ] **Step 3: Commit**

```bash
git add apps/web/src/app/dashboard/users/[id]/_components/branches-tab.tsx apps/web/src/app/dashboard/users/[id]/page.tsx
git commit -m "feat(users): BranchesTab com link/unlink"
```

---

### Task 13: ActivityTab

**Files:**
- Modify: `apps/web/src/app/dashboard/users/[id]/_components/activity-tab.tsx`

- [ ] **Step 1: Buscar dados no server**

```tsx
import { EntityAuditLogTable } from "@/components/entity/entity-audit-log-table";

import { getUserActivity } from "../../data";

const ACTION_LABELS: Record<string, string> = {
	"user.approved": "Aprovou usuário",
	"user.rejected": "Rejeitou usuário",
	"user.updated": "Atualizou usuário",
	"user.suspended": "Suspendeu usuário",
	"user.reactivated": "Reativou usuário",
	"user.deleted": "Deletou usuário",
	"user.password_reset_triggered": "Enviou reset de senha",
	"user.session_revoked": "Revogou sessão",
	"user.all_sessions_revoked": "Revogou todas as sessões",
	"user.branch_linked": "Vinculou filial",
	"user.branch_unlinked": "Desvinculou filial",
	// ferramentas (instrumentadas em task posterior)
	"tool.created": "Criou ferramenta",
	"tool.updated": "Atualizou ferramenta",
	"tool.deleted": "Deletou ferramenta",
};

export async function ActivityTab({ userId }: { userId: string }) {
	const page = await getUserActivity(userId, null, 25);
	return (
		<EntityAuditLogTable
			entries={page.items.map((it) => ({
				id: it.id,
				at: it.createdAt,
				action: it.action,
				actor: { id: userId, name: "Este usuário", type: "user" as const },
				target: it.targetId
					? { label: `${it.targetType ?? "—"} · ${it.targetId.slice(0, 8)}` }
					: undefined,
				before: null,
				after: it.metadata,
			}))}
			actionLabels={ACTION_LABELS}
			emptyMessage="Sem atividade registrada"
		/>
	);
}
```

Paginação adicional pode vir depois — primeira página (25 itens) é suficiente para a maioria dos users.

- [ ] **Step 2: check-types + smoke** PASS.

- [ ] **Step 3: Commit**

```bash
git add apps/web/src/app/dashboard/users/[id]/_components/activity-tab.tsx
git commit -m "feat(users): ActivityTab com audit log table"
```

---

### Task 14: SessionsTab

**Files:**
- Modify: `apps/web/src/app/dashboard/users/[id]/_components/sessions-tab.tsx`

- [ ] **Step 1: Implementar**

Server Component que busca sessões + sub-componente Client para o botão "Revogar":

```tsx
// sessions-tab.tsx
import { getUserSessions } from "../../data";
import { SessionsList } from "./sessions-list";

export async function SessionsTab({ userId }: { userId: string }) {
	const sessions = await getUserSessions(userId);
	return <SessionsList userId={userId} sessions={sessions} />;
}
```

```tsx
// sessions-list.tsx (novo arquivo Client Component dentro do mesmo _components/)
"use client";

import { Badge } from "@emach/ui/components/badge";
import { Button } from "@emach/ui/components/button";
import { Card, CardContent, CardHeader, CardTitle } from "@emach/ui/components/card";
import { LogOut } from "lucide-react";
import { useTransition } from "react";
import { toast } from "sonner";

import { revokeUserSession } from "../../actions";

interface Session {
	id: string;
	ipAddress: string | null;
	userAgent: string | null;
	createdAt: Date;
	expiresAt: Date;
}

const DT = new Intl.DateTimeFormat("pt-BR", { dateStyle: "short", timeStyle: "short" });

export function SessionsList({ userId, sessions }: { userId: string; sessions: Session[] }) {
	const [pending, startTransition] = useTransition();
	const revoke = (sessionId: string) =>
		startTransition(async () => {
			const res = await revokeUserSession({ sessionId });
			if (!res.ok) toast.error(res.error);
		});

	return (
		<Card>
			<CardHeader>
				<CardTitle className="text-base">Sessões ativas ({sessions.length})</CardTitle>
			</CardHeader>
			<CardContent>
				{sessions.length === 0 ? (
					<p className="text-muted-foreground text-sm">Sem sessões ativas.</p>
				) : (
					<ul className="flex flex-col gap-2">
						{sessions.map((s) => (
							<li
								key={s.id}
								className="flex items-center justify-between gap-2 rounded-md bg-muted/40 px-3 py-2"
							>
								<div className="min-w-0">
									<p className="truncate font-medium text-sm">
										{s.ipAddress ?? "IP desconhecido"}
									</p>
									<p className="truncate text-muted-foreground text-xs">
										{s.userAgent ?? "—"} · expira {DT.format(s.expiresAt)}
									</p>
								</div>
								<Button
									disabled={pending}
									onClick={() => revoke(s.id)}
									size="sm"
									variant="outline"
								>
									<LogOut className="size-3.5" />
									Revogar
								</Button>
							</li>
						))}
					</ul>
				)}
			</CardContent>
		</Card>
	);
}
```

- [ ] **Step 2: check-types + smoke** PASS.

- [ ] **Step 3: Commit**

```bash
git add apps/web/src/app/dashboard/users/[id]/_components/sessions-tab.tsx apps/web/src/app/dashboard/users/[id]/_components/sessions-list.tsx
git commit -m "feat(users): SessionsTab + revogar"
```

---

### Task 15: SecurityTab

**Files:**
- Modify: `apps/web/src/app/dashboard/users/[id]/_components/security-tab.tsx`

- [ ] **Step 1: Implementar**

```tsx
"use client";

import { Button } from "@emach/ui/components/button";
import { Card, CardContent, CardHeader, CardTitle } from "@emach/ui/components/card";
import { AlertCircle, CheckCircle2, KeyRound, LogOut } from "lucide-react";
import { useTransition } from "react";
import { toast } from "sonner";

import {
	forceLogoutAllSessions,
	triggerPasswordReset,
} from "../../actions";

interface Props {
	user: { id: string; email: string; emailVerified: boolean | null };
}

export function SecurityTab({ user }: Props) {
	const [pending, startTransition] = useTransition();
	const sendReset = () =>
		startTransition(async () => {
			const res = await triggerPasswordReset({ userId: user.id });
			if (res.ok) toast.success("E-mail de reset enviado");
			else toast.error(res.error);
		});
	const forceLogout = () =>
		startTransition(async () => {
			const res = await forceLogoutAllSessions({ userId: user.id });
			if (res.ok) toast.success("Todas as sessões foram revogadas");
			else toast.error(res.error);
		});

	return (
		<div className="flex flex-col gap-3">
			<Card>
				<CardHeader>
					<CardTitle className="text-base">Email</CardTitle>
				</CardHeader>
				<CardContent className="flex items-center gap-2">
					{user.emailVerified ? (
						<>
							<CheckCircle2 className="size-4 text-success" />
							<span className="text-sm">Email verificado</span>
						</>
					) : (
						<>
							<AlertCircle className="size-4 text-warning" />
							<span className="text-sm">Email não verificado</span>
						</>
					)}
				</CardContent>
			</Card>
			<Card>
				<CardHeader>
					<CardTitle className="text-base">Reset de senha</CardTitle>
				</CardHeader>
				<CardContent className="flex flex-col gap-3">
					<p className="text-muted-foreground text-sm">
						Envia um e-mail com link para o usuário trocar a senha. Você não terá acesso à senha nova.
					</p>
					<Button disabled={pending} onClick={sendReset} variant="outline">
						<KeyRound className="size-3.5" />
						Enviar e-mail de reset
					</Button>
				</CardContent>
			</Card>
			<Card>
				<CardHeader>
					<CardTitle className="text-base">Sessões</CardTitle>
				</CardHeader>
				<CardContent className="flex flex-col gap-3">
					<p className="text-muted-foreground text-sm">
						Revoga todas as sessões ativas — o usuário será forçado a logar de novo em todos os dispositivos.
					</p>
					<Button disabled={pending} onClick={forceLogout} variant="outline">
						<LogOut className="size-3.5" />
						Forçar logout em tudo
					</Button>
				</CardContent>
			</Card>
		</div>
	);
}
```

- [ ] **Step 2: check-types + smoke** PASS.

- [ ] **Step 3: Commit**

```bash
git add apps/web/src/app/dashboard/users/[id]/_components/security-tab.tsx
git commit -m "feat(users): SecurityTab (reset + force logout)"
```

---

### Task 16: Sheet de edit via `?edit=1`

**Files:**
- Create: `apps/web/src/app/dashboard/users/_components/user-edit-sheet.tsx`
- Modify: `apps/web/src/app/dashboard/users/[id]/page.tsx`

- [ ] **Step 1: `user-edit-sheet.tsx`**

```tsx
"use client";

import { Input } from "@emach/ui/components/input";
import { Label } from "@emach/ui/components/label";
import { useRouter, useSearchParams } from "next/navigation";
import { useEffect, useState, useTransition } from "react";
import { toast } from "sonner";

import { EntityEditSheet } from "@/components/entity/entity-edit-sheet";
import {
	type FormIssue,
	zodIssuesToFormIssues,
} from "@/components/form-error-panel";

import { updateUser } from "../actions";
import { updateUserSchema } from "../schema";
import { BranchesCombobox } from "./branches-combobox";
import { RoleSelect } from "./role-select";
import type { BranchLite, UserRow } from "./types";

interface Props {
	user: { id: string; name: string; role: UserRow["role"]; branchIds: string[] };
	branches: BranchLite[];
}

export function UserEditSheet({ user, branches }: Props) {
	const router = useRouter();
	const params = useSearchParams();
	const open = params.get("edit") === "1";
	const [name, setName] = useState(user.name);
	const [role, setRole] = useState<UserRow["role"]>(user.role);
	const [branchIds, setBranchIds] = useState<string[]>(user.branchIds);
	const [issues, setIssues] = useState<FormIssue[]>([]);
	const [submitting, startTransition] = useTransition();

	useEffect(() => {
		if (open) {
			setName(user.name);
			setRole(user.role);
			setBranchIds(user.branchIds);
			setIssues([]);
		}
	}, [open, user]);

	const close = () => {
		const sp = new URLSearchParams(params);
		sp.delete("edit");
		const qs = sp.toString();
		router.replace(qs ? `?${qs}` : window.location.pathname, { scroll: false });
	};

	const handleSubmit = (e: React.FormEvent<HTMLFormElement>) => {
		e.preventDefault();
		const parsed = updateUserSchema.safeParse({
			userId: user.id,
			name,
			role,
			branchIds,
		});
		if (!parsed.success) {
			setIssues(
				zodIssuesToFormIssues(parsed.error, {
					name: "Nome",
					role: "Cargo",
					branchIds: "Filiais",
				})
			);
			return;
		}
		startTransition(async () => {
			const res = await updateUser(parsed.data);
			if (res.ok) {
				toast.success("Usuário atualizado");
				close();
			} else {
				toast.error(res.error);
			}
		});
	};

	return (
		<EntityEditSheet
			description="Atualize nome, cargo e filiais"
			issues={issues}
			onOpenChange={(v) => !v && close()}
			onSubmit={handleSubmit}
			open={open}
			submitting={submitting}
			title={`Editar ${user.name}`}
		>
			<div className="flex flex-col gap-4">
				<div className="flex flex-col gap-1.5">
					<Label htmlFor="user-name">Nome</Label>
					<Input
						id="user-name"
						onChange={(e) => setName(e.target.value)}
						value={name}
					/>
				</div>
				<div className="flex flex-col gap-1.5">
					<Label>Cargo</Label>
					<RoleSelect onChange={setRole} value={role} />
				</div>
				<div className="flex flex-col gap-1.5">
					<Label>Filiais</Label>
					<BranchesCombobox
						branches={branches}
						onChange={setBranchIds}
						value={branchIds}
					/>
				</div>
			</div>
		</EntityEditSheet>
	);
}
```

(Reusa `BranchesCombobox` e `RoleSelect` existentes em `_components/`.)

- [ ] **Step 2: Adicionar ao `[id]/page.tsx`**

```tsx
import { UserEditSheet } from "../_components/user-edit-sheet";

// dentro do return, depois do EntityTabs:
<UserEditSheet
  user={{ id: user.id, name: user.name, role: user.role, branchIds: user.branchIds }}
  branches={branches}
/>
```

Carregar `branches` no server da página (já carregado para BranchesTab — reaproveitar).

- [ ] **Step 3: check-types + smoke** PASS.

- [ ] **Step 4: Commit**

```bash
git add apps/web/src/app/dashboard/users/
git commit -m "feat(users): sheet edit via ?edit=1"
```

---

### Task 17: Deletar componentes legacy

**Files:**
- Delete: `apps/web/src/app/dashboard/users/_components/users-tabs.tsx`
- Delete: `apps/web/src/app/dashboard/users/_components/active-table.tsx`
- Delete: `apps/web/src/app/dashboard/users/_components/pending-table.tsx`
- Delete: `apps/web/src/app/dashboard/users/_components/suspended-table.tsx`
- Delete: `apps/web/src/app/dashboard/users/_components/edit-sheet.tsx`

- [ ] **Step 1: Confirmar zero references**

```bash
for f in users-tabs active-table pending-table suspended-table edit-sheet; do
  echo "=== $f ==="
  grep -r "from.*_components/$f" apps/web/src
done
```

Se algum import sobrar, ajustar antes de deletar.

- [ ] **Step 2: Deletar**

```bash
cd apps/web/src/app/dashboard/users/_components
rm users-tabs.tsx active-table.tsx pending-table.tsx suspended-table.tsx edit-sheet.tsx
```

- [ ] **Step 3: check-types + smoke**

PASS. `/dashboard/users` continua funcional.

- [ ] **Step 4: Commit**

```bash
git add -A apps/web/src/app/dashboard/users/_components/
git commit -m "chore(users): remove componentes legacy"
```

---

### Task 18: Instrumentar mutations das demais features com `logUserActivity`

**Files:**
- Modify: `apps/web/src/app/dashboard/tools/actions.ts`
- Modify: `apps/web/src/app/dashboard/categories/actions.ts`
- Modify: `apps/web/src/app/dashboard/suppliers/actions.ts`
- Modify: `apps/web/src/app/dashboard/branches/actions.ts`

- [ ] **Step 1: Por feature, adicionar `logUserActivity` em mutations**

Para cada action que muta DB (create/update/delete/reorder/toggle), adicionar 1 linha após o commit principal:

| Feature | Actions a instrumentar |
|---|---|
| tools | createTool, updateTool, deleteTool, uploadToolImage, deleteToolImage, reorderToolImages |
| categories | createCategory, updateCategory, deleteCategory, toggleCategoryActive, reorderCategories |
| suppliers | createSupplier, updateSupplier, deleteSupplier |
| branches | createBranch, updateBranch, deleteBranch, setDefaultBranch |

Padrão da chamada (exemplo `createTool`):

```ts
await logUserActivity({
  actorUserId: session.user.id,
  action: "tool.created",
  targetType: "tool",
  targetId: toolId,
});
```

Mapeamento `action`: `<feature>.<verb>` em snake_case (`tool.created`, `category.updated`, `supplier.deleted`, `branch.set_default`).

Para mutations sem `actorUserId` claro (eventos automáticos), usar `actorType: "system"`. Mas todas as mutations listadas têm `requireCurrentSession` ou `requireCapability` que dá acesso ao session — usar `actorType` user (default omitido — helper trata como user já que actorUserId é required).

Não adicionar `metadata` rico aqui — escopo do PR Fase 1 cresce demais. Adicionar só nos casos óbvios (ex: `branch.set_default` → `metadata: { branchId }`).

- [ ] **Step 2: check-types**

PASS.

- [ ] **Step 3: Commit por feature (4 commits)**

```bash
git add apps/web/src/app/dashboard/tools/actions.ts
git commit -m "feat(tools): logUserActivity nas mutations"

git add apps/web/src/app/dashboard/categories/actions.ts
git commit -m "feat(categories): logUserActivity nas mutations"

git add apps/web/src/app/dashboard/suppliers/actions.ts
git commit -m "feat(suppliers): logUserActivity nas mutations"

git add apps/web/src/app/dashboard/branches/actions.ts
git commit -m "feat(branches): logUserActivity nas mutations"
```

---

### Task 19: Impeccable audit + polish

**Files:**
- Iterar nos componentes novos da lista + detalhe.

- [ ] **Step 1: Visitar `/dashboard/users` e `/dashboard/users/<id>` em dev**

```bash
bun dev:web
```

- [ ] **Step 2: Aplicar polish checklist da Fase 0**

- Status badges: ícone + label + cor (Task 8 step 1 já cobre).
- Empty states: ícone `size-12 opacity-40`.
- Sem borders extras em cards (Card primitive já faz surface).
- `lucide-react` only.
- `font-medium` default; `font-semibold` só em `PageHeader` e h2 de seção.

- [ ] **Step 3: Commits granulares por ajuste**

`style(users): <ajuste específico>`. Iterar.

---

### Task 20: Verificação final + push gated

- [ ] **Step 1: Testes**

```bash
bun --cwd apps/web test
```

Expected: PASS (incluindo o novo `permissions.test.ts` da Task 1).

- [ ] **Step 2: check-types**

```bash
bun check-types
```

Expected: PASS.

- [ ] **Step 3: Lint**

```bash
cd apps/web && bunx ultracite check src/app/dashboard/users src/app/dashboard/tools/actions.ts src/app/dashboard/categories/actions.ts src/app/dashboard/suppliers/actions.ts src/app/dashboard/branches/actions.ts
```

Expected: 0 errors.

- [ ] **Step 4: Smoke run**

```bash
bun dev:web
```

Visitar:
- `/dashboard/users` — KPIs + pending + activity + grid OK.
- `/dashboard/users/<id>` — 5 tabs OK, URL state via `?tab=`.
- `/dashboard/users/<id>?edit=1` — sheet abre.
- Reset senha → toast sucesso.
- Force logout → toast sucesso.
- Link/unlink branch → estado atualiza.

- [ ] **Step 5: Confirmar com usuário**

`git log --oneline origin/main..HEAD` — listar os ~25 commits da fase. Aguardar aprovação explícita.

- [ ] **Step 6: Push (APÓS aprovação)**

```bash
git push -u origin fase-1-users
gh pr create --title "feat: Fase 1 — Users CRUD completo" --body "..."
```

---

## Self-review checklist

- [x] Capability `users.revoke_sessions` — Task 1
- [x] Better Auth `lastLoginAt` hook — Task 2
- [x] `triggerPasswordReset` substitui plaintext — Task 3
- [x] Instrumentação `logUserActivity` em mutations de users — Task 4
- [x] `revokeUserSession` + `forceLogoutAllSessions` — Task 5
- [x] `linkUserToBranch` / `unlinkUserFromBranch` — Task 6
- [x] Data fetchers (`getUserKpis`, `fetchUsersPage`, `getUserDetail`, etc) — Task 7
- [x] Lista refeita (KPIs + pending + activity + grid) — Task 8
- [x] Status tabs + cursor pagination — Task 9
- [x] Detalhe esqueleto (5 tabs stubs) — Task 10
- [x] ProfileTab — Task 11
- [x] BranchesTab — Task 12
- [x] ActivityTab — Task 13
- [x] SessionsTab — Task 14
- [x] SecurityTab — Task 15
- [x] UserEditSheet — Task 16
- [x] Cleanup legacy — Task 17
- [x] Instrumentação cross-feature — Task 18
- [x] Polish audit — Task 19
- [x] Verificação final + push gated — Task 20

## Trade-offs e TODOs deixados para Fase 2/futura

- **Guard "último admin"** ao desvincular branch — não implementado nessa fase (mencionado na Task 6).
- **Audit `actorName` na tab Atividade** — usa "Este usuário" hardcoded; futuro pode buscar nome do target.
- **Paginação extra na ActivityTab** — primeira página de 25 itens; load more virá com infinite scroll quando necessário.
- **Metadata rico no logUserActivity cross-feature** — instrumentação básica; refinement depois conforme uso.
